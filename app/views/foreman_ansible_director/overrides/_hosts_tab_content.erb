<%= webpacked_plugins_js_for :foreman_ansible_director %>

<div class='tab-pane' id='ansible_director'>
  <%= react_component('AnsibleContentAssignmentMain', {
    dataInterface: f.object.persisted? ? 'api': 'dom',
    crnType: "Host",
    crnId: f.object.id,
    crnName: f.object.name,
    contentSource: ::ForemanAnsibleDirector::AssignmentService.content_source_for(f.object)[0],
  }) %>
</div>

