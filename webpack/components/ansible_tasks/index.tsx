import React, { ReactElement } from 'react';
import { AnsibleTasksPageWrapper } from './AnsibleTasksPageWrapper';

export const WrappedAnsibleTasksPage = (): ReactElement => (
  <AnsibleTasksPageWrapper />
);
